<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for any network traffic -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- VPN / foreground service requirements (per flutter_v2ray docs) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Android 13+ notifications (optional but recommended) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Media features -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Android 13+ (Tiramisu) images permission -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Android 13+ audio permission (voice notes) -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Android 13+ video permission -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <!-- Android 12 and below fallback -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <!-- Android 9 and below write permission (legacy) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <!-- Allow auto-start scenarios if you later add it -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Needed by some per-app tunneling implementations to enumerate packages -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- Vibration for calls -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Wake lock for background service / calls -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- [UPDATE #2] Bluetooth for WebRTC audio routing during calls -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- [UPDATE #3] Camera for video calls -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name="${applicationName}"
        android:label="CDN-NETCHAT"
        android:icon="@mipmap/ic_launcher"
        android:extractNativeLibs="true">

        <activity
            android:name="cdn.example.cdn_chatroom.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- flutter_v2ray VPN Service (required for V2Ray core) -->
        <service
            android:name="com.v2ray.ang.service.V2RayServiceManager"
            android:exported="false"
            android:foregroundServiceType="systemExempted"
            android:process=":vpn_core" />

        <!-- Image Cropper Activity -->
        <activity
            android:name="com.lykni.image_cropper.ImageCropperActivity"
            android:exported="false"
            android:theme="@style/Theme.ImageCropper" />

        <!-- [UPDATE #2] Firebase Cloud Messaging — default notification channel -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="messages" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/accent" />

        <!-- [UPDATE #2] FCM background message handler (flutter_local_notifications + firebase_messaging) -->
        <!-- Ensures notifications arrive even when app is terminated -->
        <service
            android:name="com.dexterous.flutterlocalnotifications.ForegroundService"
            android:exported="false"
            android:stopWithTask="false" />

        <!-- [UPDATE #2] Firebase Messaging Service for high-priority FCM messages -->
        <service
            android:name="com.google.firebase.messaging.FirebaseMessagingService"
            android:exported="false">
            <intent-filter android:priority="1000">
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>
</manifest>
